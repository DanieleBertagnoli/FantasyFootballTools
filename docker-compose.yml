services:
    fantasy-football-tools:
        container_name: fantasy-football-tools
        restart: always
        build: .
        ports:
            - "5010:5010"
        environment:
            FLASK_SECRET_KEY: ${FLASK_SECRET_KEY:-local-development-key-change-before-deployment}
